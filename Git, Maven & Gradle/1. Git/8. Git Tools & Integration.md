## Overview

Modern Git workflows leverage powerful tools and integrations to boost productivity. This guide covers GUI tools, command-line enhancements, and platform features for professional Java development.

```
┌────────────────────────────────────────────────────────┐
│              GIT TOOLS ECOSYSTEM                       │
├────────────────────────────────────────────────────────┤
│                                                        │
│  GUI TOOLS                                             │
│  ├─ GitKraken     (Visual commit graph)               │
│  ├─ SourceTree    (Free, feature-rich)                │
│  ├─ GitHub Desktop (Simple, GitHub-focused)           │
│  └─ IDE Integration (IntelliJ, VS Code)               │
│                                                        │
│  CLI ENHANCEMENTS                                      │
│  ├─ Git Aliases   (Shortcuts for common commands)     │
│  ├─ GitHub CLI    (gh pr create, gh issue list)       │
│  └─ Custom Scripts (Automation)                       │
│                                                        │
│  PLATFORM FEATURES                                     │
│  ├─ Issues & Projects (Track work)                    │
│  ├─ CI/CD Actions   (Automate workflows)              │
│  ├─ Protected Branches (Enforce rules)                │
│  ├─ CODEOWNERS     (Review requirements)              │
│  └─ PR Templates   (Standardize reviews)              │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 1. Git GUIs

### GitKraken

Professional Git client with visual commit graph and team collaboration features.

**Features:**

```
GitKraken Advantages:
─────────────────────

✓ Beautiful visual commit graph
✓ Drag-and-drop branch operations
✓ Built-in merge conflict editor
✓ GitHub/GitLab/Bitbucket integration
✓ Interactive rebase interface
✓ Undo functionality
✓ Team features (shared profiles)
✓ Cross-platform (Windows, Mac, Linux)

GitKraken Disadvantages:
────────────────────────

✗ Paid (free for students/open source)
✗ Can be resource-intensive
✗ Some advanced features require Pro
```

**Common workflows in GitKraken:**

```
1. Visual Commit Graph
   ───────────────────
   • See entire branch structure at a glance
   • Identify merge points and divergences
   • Understand team workflow visually

2. Drag-and-Drop Branching
   ────────────────────────
   • Drag branch to another to merge
   • Right-click for rebase options
   • Visual feedback for operations

3. Interactive Rebase
   ──────────────────
   • Visual interface for commit reordering
   • Squash, drop, or edit commits
   • No command-line syntax required

4. Conflict Resolution
   ───────────────────
   • Built-in 3-way merge editor
   • Side-by-side comparison
   • Choose "ours," "theirs," or manual edit

5. Pull Request Management
   ────────────────────────
   • Create PRs from GitKraken
   • View PR status and checks
   • Review and comment inline
```

---

### SourceTree

Free Git GUI by Atlassian with powerful features.

**Features:**

```
SourceTree Advantages:
──────────────────────

✓ FREE (no limitations)
✓ Powerful visual interface
✓ Git Flow integration
✓ Interactive rebase
✓ File history visualization
✓ Stash management
✓ Submodule support
✓ Bitbucket integration

SourceTree Disadvantages:
─────────────────────────

✗ Requires Atlassian account
✗ Mac and Windows only (no Linux)
✗ Can be slow with large repos
```

**Key SourceTree features:**

```
1. Git Flow Support
   ────────────────
   • Initialize Git Flow with one click
   • Automatic feature/release/hotfix branches
   • Visual Git Flow workflow

2. Interactive Staging
   ────────────────────
   • Stage individual hunks
   • Visual diff while staging
   • Discard changes easily

3. Bookmarks
   ─────────
   • Save frequently used repos
   • Quick switching between projects
   • Organize by team or project

4. File History
   ────────────
   • View all changes to specific file
   • Blame view with commit details
   • Compare versions side-by-side
```

---

### GitHub Desktop

Simple, streamlined Git client focused on GitHub workflows.

**Features:**

```
GitHub Desktop Advantages:
──────────────────────────

✓ FREE and open source
✓ Very simple and intuitive
✓ Tight GitHub integration
✓ Auto-detection of text editors/IDEs
✓ Fast and lightweight
✓ Cross-platform
✓ Great for beginners

GitHub Desktop Disadvantages:
──────────────────────────────

✗ Limited advanced features
✗ Focused mainly on GitHub
✗ No built-in merge conflict editor
✗ Less suitable for complex workflows
```

**Typical GitHub Desktop workflow:**

```
1. Clone Repository
   ────────────────
   File → Clone Repository
   Select from GitHub repos
   Choose local path

2. Create Branch
   ─────────────
   Branch → New Branch
   Name branch
   Automatically switches

3. Make Changes
   ────────────
   Edit files in chosen editor
   Changes appear automatically
   View diff in GitHub Desktop

4. Commit
   ──────
   Write commit message
   Choose files to include
   Commit to current branch

5. Push to GitHub
   ──────────────
   Publish branch (first time)
   Push origin (subsequent)

6. Create Pull Request
   ───────────────────
   Branch → Create Pull Request
   Opens browser to GitHub
   Fill in PR details
```

---

### IDE Integration (IntelliJ IDEA, VS Code)

**IntelliJ IDEA Git Integration:**

```
Git Features in IntelliJ IDEA:
───────────────────────────────

1. VCS Operations Panel
   ────────────────────
   • View changes by file
   • Stage/unstage files
   • Commit with message
   • View local changes diff

2. Commit Tool Window
   ──────────────────
   • Shows all modified files
   • Built-in diff viewer
   • Commit message template
   • Code analysis before commit
   • Reformat code option
   • Run tests before commit

3. Git Log
   ───────
   • Visual commit graph
   • Filter by branch, author, date
   • Search commit messages
   • View file history
   • Annotate (blame) view

4. Branch Management
   ─────────────────
   • Create/switch branches from status bar
   • Visual branch hierarchy
   • Compare branches
   • Merge/Rebase from menu

5. Conflict Resolution
   ───────────────────
   • 3-way merge tool
   • Accept yours/theirs/both
   • Inline conflict markers
   • Conflict navigation

6. Keyboard Shortcuts
   ──────────────────
   Cmd/Ctrl + K       Commit changes
   Cmd/Ctrl + T       Update project (pull)
   Cmd/Ctrl + Shift + K  Push commits
   Alt + 9            Show Version Control tool
   Cmd/Ctrl + Alt + Z   Rollback changes
```

**IntelliJ IDEA Git workflow:**

```bash
# 1. Make changes to files
# IntelliJ highlights modified files in blue

# 2. View changes
# Cmd/Ctrl + D on file to view diff

# 3. Commit
# Cmd/Ctrl + K
# - Select files to commit
# - Write message
# - Run code inspection
# - Reformat code
# - Click Commit or Commit and Push

# 4. Push
# Cmd/Ctrl + Shift + K
# - Review commits to push
# - Click Push

# 5. Update (Pull)
# Cmd/Ctrl + T
# - Choose merge or rebase
# - Click OK

# 6. Resolve conflicts
# - IntelliJ detects conflicts
# - Opens merge tool automatically
# - Accept changes
# - Click Apply
```

**VS Code Git Integration:**

```
Git Features in VS Code:
────────────────────────

1. Source Control Panel
   ────────────────────
   • View all changes
   • Stage/unstage files
   • Commit with message
   • Sync with remote

2. Git Graph Extension
   ───────────────────
   • Visual commit history
   • Branch visualization
   • Interactive operations
   • Install: code --install-extension mhutchie.git-graph

3. GitLens Extension
   ─────────────────
   • Inline blame annotations
   • File history
   • Compare commits
   • Rich commit details
   • Install: code --install-extension eamodio.gitlens

4. Built-in Features
   ─────────────────
   • Diff editor
   • Merge conflict resolution
   • Timeline view
   • Branch management in status bar

5. Keyboard Shortcuts
   ──────────────────
   Ctrl/Cmd + Shift + G   Open Source Control
   Ctrl/Cmd + Enter       Commit staged
   Ctrl/Cmd + K, Ctrl/Cmd + Shift + S  Stage all
```

**VS Code Git workflow:**

```bash
# 1. View changes
# Click Source Control icon (Ctrl/Cmd + Shift + G)

# 2. Stage changes
# Click + next to file
# Or stage all with Ctrl/Cmd + K, Ctrl/Cmd + Shift + S

# 3. Commit
# Write message in input box
# Ctrl/Cmd + Enter to commit

# 4. Push
# Click ... menu → Push
# Or click sync icon in status bar

# 5. Pull
# Click ... menu → Pull
# Or click sync icon

# 6. Branch operations
# Click branch name in status bar
# Select operation (create, switch, merge)
```

---

### When to Use GUI vs CLI

**Decision Matrix:**

```
Use GUI when:
─────────────

✓ Visualizing commit history
  • Understanding branch relationships
  • Exploring project timeline
  • Finding merge points

✓ Resolving merge conflicts
  • 3-way merge visualization
  • Side-by-side comparison
  • Point-and-click resolution

✓ Interactive rebase
  • Reordering commits visually
  • Squashing with drag-and-drop
  • Editing commit messages

✓ Learning Git
  • Understanding concepts visually
  • Seeing immediate feedback
  • Reducing command memorization

✓ Complex branch operations
  • Comparing multiple branches
  • Cherry-picking visually
  • Managing multiple remotes

Use CLI when:
─────────────

✓ Scripting and automation
  • CI/CD pipelines
  • Git hooks
  • Batch operations

✓ Remote server access (SSH)
  • No GUI available
  • Server-side operations
  • Production deployments

✓ Speed and efficiency
  • Faster for experienced users
  • Keyboard-focused workflow
  • No mouse required

✓ Advanced operations
  • Complex git commands
  • Bisect debugging
  • Filter-branch operations

✓ Precise control
  • Exact command parameters
  • Reproducible operations
  • Documentation purposes

Hybrid Approach (Best):
───────────────────────

✓ Use GUI for visualization and conflict resolution
✓ Use CLI for daily commits and common operations
✓ Use IDE integration for code-centric tasks
✓ Learn both to maximize productivity
```

**Example workflows:**

```
Scenario 1: Daily Development
──────────────────────────────
• Edit code in IDE (IntelliJ)
• View changes in IDE Git panel
• Commit using IDE (Cmd+K)
• Push using IDE (Cmd+Shift+K)
Why: Fast, integrated with coding

Scenario 2: Resolving Complex Merge
────────────────────────────────────
• git fetch origin (CLI)
• git merge origin/main (CLI - conflict!)
• Open GitKraken/IntelliJ merge tool
• Resolve conflicts visually
• git add . && git commit (CLI)
Why: Visual conflict resolution is easier

Scenario 3: Understanding History
──────────────────────────────────
• Open SourceTree/GitKraken
• View commit graph
• Explore branches visually
• Find problematic commit
• git show <sha> (CLI for details)
Why: Visual graph shows relationships

Scenario 4: CI/CD Pipeline
───────────────────────────
• git clone repo
• git checkout -b feature/new
• git add . && git commit -m "..."
• git push -u origin feature/new
Why: Automation requires CLI

Scenario 5: Code Review
────────────────────────
• git fetch origin
• git checkout pr-branch
• View changes in IDE
• Test code
• Comment on GitHub web UI
Why: Best tool for each step
```

---

## 2. Git Aliases

### Creating Shortcuts

**Configuration syntax:**

```bash
# Create alias with git config
git config --global alias.<alias-name> '<git-command>'

# Examples:
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status

# Now use:
git co main        # git checkout main
git br             # git branch
git ci -m "msg"    # git commit -m "msg"
git st             # git status
```

**Editing .gitconfig directly:**

```bash
# Open .gitconfig
vim ~/.gitconfig

# Add aliases section:
[alias]
    co = checkout
    br = branch
    ci = commit
    st = status
    unstage = reset HEAD --
    last = log -1 HEAD
    visual = log --graph --oneline --all
```

---

### Useful Aliases

**Essential aliases:**

```bash
# ~/.gitconfig

[alias]
    # ─────────────────────────────────────────
    # Basic shortcuts
    # ─────────────────────────────────────────
    
    co = checkout
    br = branch
    ci = commit
    st = status
    
    # ─────────────────────────────────────────
    # Status shortcuts
    # ─────────────────────────────────────────
    
    s = status -sb              # Short status with branch
    
    # ─────────────────────────────────────────
    # Commit shortcuts
    # ─────────────────────────────────────────
    
    cm = commit -m              # Commit with message
    ca = commit -am             # Add all and commit
    amend = commit --amend      # Amend last commit
    amendne = commit --amend --no-edit  # Amend without editing message
    
    # ─────────────────────────────────────────
    # Log variations
    # ─────────────────────────────────────────
    
    lg = log --graph --oneline --decorate --all
    ls = log --pretty=format:"%C(yellow)%h %C(blue)%ad %C(red)%d %C(reset)%s %C(green)[%cn]" --decorate --date=short
    ll = log --pretty=format:"%C(yellow)%h%C(red)%d %C(reset)%s %C(green)[%cn]" --decorate --numstat
    last = log -1 HEAD --stat
    dl = "!git ll -1"           # Show last commit details
    
    # ─────────────────────────────────────────
    # Diff shortcuts
    # ─────────────────────────────────────────
    
    d = diff
    ds = diff --staged
    dc = diff --cached
    
    # ─────────────────────────────────────────
    # Branch operations
    # ─────────────────────────────────────────
    
    branches = branch -a        # Show all branches
    remotes = remote -v         # Show remotes
    current = rev-parse --abbrev-ref HEAD  # Show current branch
    
    # ─────────────────────────────────────────
    # Undo operations
    # ─────────────────────────────────────────
    
    unstage = reset HEAD --     # Unstage files
    undo = reset --soft HEAD~1  # Undo last commit, keep changes
    discard = checkout --       # Discard changes
    
    # ─────────────────────────────────────────
    # Stash shortcuts
    # ─────────────────────────────────────────
    
    stash-all = stash save --include-untracked
    stash-unapply = !git stash show -p | git apply -R
    
    # ─────────────────────────────────────────
    # Advanced operations
    # ─────────────────────────────────────────
    
    # List contributors with commit counts
    contributors = shortlog -sn
    
    # Show files in a commit
    show-files = "!f() { git diff-tree --no-commit-id --name-only -r $1; }; f"
    
    # Delete merged branches
    cleanup = "!git branch --merged | grep -v '\\*\\|main\\|develop' | xargs -n 1 git branch -d"
    
    # Find commits by commit message
    find = "!f() { git log --all --grep=$1; }; f"
    
    # Find commits by source code
    fc = "!f() { git log --pretty=format:'%C(yellow)%h %C(blue)%ad %C(reset)%s%C(green) [%cn]' --decorate --date=short -S$1; }; f"
    
    # Create new branch and switch
    nb = "!f() { git checkout -b $1; }; f"
    
    # Sync with remote
    sync = !git fetch origin && git rebase origin/main
    
    # Quick commit
    quick = "!f() { git add -A && git commit -m \"$1\"; }; f"
    
    # Pretty log with graph
    tree = log --graph --pretty=format:'%C(auto)%h%d %s %C(black)%C(bold)%cr' --all
    
    # Show ignored files
    ignored = !git ls-files -v | grep "^[[:lower:]]"
    
    # Assume unchanged (useful for config files)
    assume = update-index --assume-unchanged
    unassume = update-index --no-assume-unchanged
    assumed = "!git ls-files -v | grep ^h | cut -c 3-"
```

**Usage examples:**

```bash
# Using aliases

# Status
git s
# On main | +1 -0

# Quick commit
git quick "feat: Add user validation"
# Equivalent to: git add -A && git commit -m "feat: Add user validation"

# Pretty log
git lg
# * a1b2c3d (HEAD -> main) feat: Add validation
# * 9a8b7c6 fix: Correct bug
# * 8a7b6c5 docs: Update README

# Find commits
git find "authentication"
# Shows all commits with "authentication" in message

# Sync with remote
git sync
# Fetches and rebases on origin/main

# Show last commit
git last
# commit a1b2c3d...
# Author: John Doe
# Date: Mon Jan 15 2024
#     feat: Add user validation

# Cleanup merged branches
git cleanup
# Deleted branch: feature/old-feature
# Deleted branch: bugfix/old-bug
```

**Complex alias examples:**

```bash
# ~/.gitconfig

[alias]
    # Show commits in current branch not in main
    new = !git log main..HEAD
    
    # Interactive rebase with specific number of commits
    rb = "!f() { git rebase -i HEAD~$1; }; f"
    # Usage: git rb 5  (rebase last 5 commits)
    
    # Publish current branch to remote
    publish = !git push -u origin $(git current)
    
    # Unpublish (delete remote branch)
    unpublish = !git push origin :$(git current)
    
    # Pull with rebase and autostash
    up = pull --rebase --autostash
    
    # Save current work (stash with message and untracked)
    save = !git stash save --include-untracked
    
    # Restore last stash
    restore = stash pop
    
    # Show what you've done today
    today = log --since="yesterday" --author="$(git config user.name)" --oneline
    
    # Show what changed in last commit
    changed = diff --name-only HEAD~1..HEAD
    
    # Checkout pull request (GitHub)
    pr = "!f() { git fetch origin pull/$1/head:pr-$1 && git checkout pr-$1; }; f"
    # Usage: git pr 123  (checkout PR #123)
```

---

## 3. GitHub/GitLab Features

### Issues and Project Boards

**GitHub Issues:**

```markdown
# Creating an issue

## Issue Template
---
name: Bug Report
about: Report a bug in the application
title: '[BUG] '
labels: bug
assignees: ''
---

**Describe the bug**
A clear description of the bug.

**To Reproduce**
Steps to reproduce:
1. Go to '...'
2. Click on '....'
3. See error

**Expected behavior**
What should happen.

**Screenshots**
If applicable, add screenshots.

**Environment:**
- OS: [e.g. macOS 13]
- Java Version: [e.g. 17]
- Spring Boot Version: [e.g. 3.2.0]

**Additional context**
Any other relevant information.
```

**Issue labels:**

```
bug          - Something isn't working
feature      - New feature request
documentation - Documentation improvements
enhancement  - Improvement to existing feature
help wanted  - Extra attention needed
good first issue - Good for newcomers
wontfix      - This will not be worked on
duplicate    - This issue already exists
invalid      - This doesn't seem right
question     - Further information requested
priority:high - High priority
priority:medium - Medium priority
priority:low - Low priority
```

**GitHub Projects (Kanban boards):**

```
Project Board Columns:
──────────────────────

📋 Backlog
   - Issues not yet started
   - Feature requests
   - Future enhancements

📝 To Do
   - Ready to work
   - Prioritized
   - Assigned

🏗 In Progress
   - Currently being worked on
   - Has assignee
   - Actively developed

👀 In Review
   - Pull request open
   - Awaiting review
   - CI/CD running

✅ Done
   - Merged to main
   - Deployed
   - Closed

🚫 Blocked
   - Waiting on dependencies
   - Needs discussion
   - External blockers
```

---

### Actions/CI Integration

**GitHub Actions workflow for Java:**

```yaml
# .github/workflows/ci.yml
name: Java CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
      
      - name: Build with Maven
        run: mvn -B clean package
      
      - name: Run tests
        run: mvn test
      
      - name: Generate coverage report
        run: mvn jacoco:report
      
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          files: ./target/site/jacoco/jacoco.xml
      
      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
      
      - name: Build Docker image
        run: docker build -t myapp:${{ github.sha }} .
      
      - name: Save artifacts
        uses: actions/upload-artifact@v3
        with:
          name: jar-file
          path: target/*.jar
```

**GitLab CI/CD (.gitlab-ci.yml):**

```yaml
# .gitlab-ci.yml
variables:
  MAVEN_OPTS: "-Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository"

cache:
  paths:
    - .m2/repository
    - target/

stages:
  - build
  - test
  - quality
  - deploy

build:
  stage: build
  image: maven:3.9-eclipse-temurin-17
  script:
    - mvn clean compile
  artifacts:
    paths:
      - target/

test:
  stage: test
  image: maven:3.9-eclipse-temurin-17
  script:
    - mvn test
    - mvn jacoco:report
  coverage: '/Total.*?([0-9]{1,3})%/'
  artifacts:
    reports:
      junit: target/surefire-reports/TEST-*.xml
      coverage_report:
        coverage_format: cobertura
        path: target/site/jacoco/jacoco.xml

quality:
  stage: quality
  image: maven:3.9-eclipse-temurin-17
  script:
    - mvn sonar:sonar
      -Dsonar.projectKey=$CI_PROJECT_NAME
      -Dsonar.host.url=$SONAR_HOST_URL
      -Dsonar.login=$SONAR_TOKEN
  only:
    - merge_requests
    - main

deploy:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  script:
    - docker build -t myapp:$CI_COMMIT_SHA .
    - docker push myapp:$CI_COMMIT_SHA
  only:
    - main
```

---

### GitHub CLI (gh)

**Installation and setup:**

```bash
# Install GitHub CLI
# macOS
brew install gh

# Ubuntu/Debian
sudo apt install gh

# Windows
winget install GitHub.cli

# Authenticate
gh auth login
```

**Common gh commands:**

```bash
# ─────────────────────────────────────────
# Repository operations
# ─────────────────────────────────────────

# Clone repository
gh repo clone user/repo

# Create repository
gh repo create my-new-repo --public --source=. --remote=origin

# View repository
gh repo view

# Fork repository
gh repo fork user/repo

# ─────────────────────────────────────────
# Pull Request operations
# ─────────────────────────────────────────

# Create PR
gh pr create --title "Add authentication" --body "Implements JWT auth"

# Create PR interactively
gh pr create

# List PRs
gh pr list

# View PR
gh pr view 123

# Check out PR locally
gh pr checkout 123

# Review PR
gh pr review 123 --approve
gh pr review 123 --comment --body "Looks good!"
gh pr review 123 --request-changes --body "Please fix..."

# Merge PR
gh pr merge 123 --squash
gh pr merge 123 --rebase
gh pr merge 123 --merge

# Close PR
gh pr close 123

# Reopen PR
gh pr reopen 123

# View PR diff
gh pr diff 123

# View PR checks
gh pr checks 123

# ─────────────────────────────────────────
# Issue operations
# ─────────────────────────────────────────

# Create issue
gh issue create --title "Bug in auth" --body "Description"

# Create issue interactively
gh issue create

# List issues
gh issue list

# View issue
gh issue view 456

# Close issue
gh issue close 456

# Reopen issue
gh issue reopen 456

# Add label
gh issue edit 456 --add-label "bug"

# Assign issue
gh issue edit 456 --add-assignee "@me"

# ─────────────────────────────────────────
# Release operations
# ─────────────────────────────────────────

# Create release
gh release create v1.0.0 --title "Release 1.0.0" --notes "First release"

# Upload asset to release
gh release upload v1.0.0 myapp.jar

# List releases
gh release list

# View release
gh release view v1.0.0

# Download release assets
gh release download v1.0.0

# ─────────────────────────────────────────
# Workflow (Actions) operations
# ─────────────────────────────────────────

# List workflows
gh workflow list

# View workflow runs
gh run list

# View specific run
gh run view 12345

# Watch run
gh run watch 12345

# Rerun workflow
gh run rerun 12345

# ─────────────────────────────────────────
# Gist operations
# ─────────────────────────────────────────

# Create gist
gh gist create myfile.java --public

# List gists
gh gist list

# View gist
gh gist view abc123
```

**Advanced gh usage:**

```bash
# View PR as JSON (for scripting)
gh pr view 123 --json title,body,number,author

# Filter and format with jq
gh pr list --json number,title | jq '.[] | select(.title | contains("bug"))'

# Create PR with template
gh pr create --template .github/PULL_REQUEST_TEMPLATE.md

# Bulk operations
gh pr list --state open --json number --jq '.[].number' | xargs -I {} gh pr merge {}

# Set default editor
export EDITOR=vim
gh pr create  # Opens vim for PR description

# Create workflow dispatch event
gh workflow run deploy.yml --ref main
```

---

### Protected Branches

**GitHub protected branch settings:**

```
Repository Settings → Branches → Branch protection rules

Branch name pattern: main

☑ Require pull request reviews before merging
  Number of required approvals: 2
  ☑ Dismiss stale pull request approvals when new commits are pushed
  ☑ Require review from Code Owners
  ☑ Restrict who can dismiss pull request reviews
      Specify: tech-leads team

☑ Require status checks to pass before merging
  ☑ Require branches to be up to date before merging
  Required status checks:
    ☑ continuous-integration/travis-ci
    ☑ build
    ☑ test
    ☑ security-scan

☑ Require conversation resolution before merging

☑ Require signed commits

☑ Require linear history

☑ Include administrators

☑ Restrict pushes
  Specify people, teams, or apps:
    - tech-leads team
    - ci-bot

☑ Allow force pushes
  Specify who: [empty - no one]

☑ Allow deletions
  [unchecked]
```

**GitLab protected branches:**

```
Repository Settings → Repository → Protected Branches

Branch: main

Allowed to merge:
  - Maintainers

Allowed to push:
  - No one

Allowed to force push:
  - [ ] Disabled

Code owner approval:
  - [x] Required
```

---

### CODEOWNERS

**GitHub CODEOWNERS file:**

```bash
# .github/CODEOWNERS

# Global owners (review all PRs)
* @tech-lead @architect

# ─────────────────────────────────────────
# Backend code
# ─────────────────────────────────────────

# All Java code
*.java @backend-team @java-experts

# Spring configuration
src/main/resources/*.yml @backend-team @config-team
src/main/resources/*.properties @backend-team @config-team

# ─────────────────────────────────────────
# Specific modules
# ─────────────────────────────────────────

# Authentication module (requires security review)
/src/main/java/auth/ @security-team @backend-team
/src/test/java/auth/ @security-team @backend-team

# Payment processing (requires payment + security review)
/src/main/java/payment/ @payment-team @security-team
/src/test/java/payment/ @payment-team @security-team

# User management
/src/main/java/user/ @user-team @backend-team

# API endpoints
/src/main/java/controller/ @api-team @backend-team

# ─────────────────────────────────────────
# Database
# ─────────────────────────────────────────

# Database migrations (requires DBA review)
/src/main/resources/db/migration/ @dba-team @backend-team

# JPA entities
/src/main/java/entity/ @database-team @backend-team

# ─────────────────────────────────────────
# Testing
# ─────────────────────────────────────────

# Integration tests
/src/test/java/integration/ @qa-team @backend-team

# Test configuration
/src/test/resources/ @qa-team

# ─────────────────────────────────────────
# Infrastructure
# ─────────────────────────────────────────

# Docker
/Dockerfile @devops-team
/docker-compose*.yml @devops-team

# Kubernetes
/k8s/ @devops-team @sre-team

# CI/CD
/.github/workflows/ @devops-team
/.gitlab-ci.yml @devops-team

# Terraform
/terraform/ @infrastructure-team @devops-team

# ─────────────────────────────────────────
# Build configuration
# ─────────────────────────────────────────

# Maven
/pom.xml @build-team @backend-team
/**/pom.xml @build-team

# Gradle
/build.gradle @build-team @backend-team
/settings.gradle @build-team

# ─────────────────────────────────────────
# Documentation
# ─────────────────────────────────────────

# README and docs
*.md @tech-writers @documentation-team
/docs/ @tech-writers @documentation-team

# API documentation
/src/main/resources/api/ @api-team @tech-writers

# ─────────────────────────────────────────
# Security
# ─────────────────────────────────────────

# Security-sensitive files (always require security review)
**/*security* @security-team
**/*auth* @security-team
**/*password* @security-team
**/*encryption* @security-team

# Secrets management
/.github/workflows/*secret*.yml @security-team @devops-team

# ─────────────────────────────────────────
# Configuration
# ─────────────────────────────────────────

# Environment configuration
.env.example @config-team
application*.yml @config-team @backend-team

# ─────────────────────────────────────────
# Special cases
# ─────────────────────────────────────────

# Only tech lead can modify CODEOWNERS
/.github/CODEOWNERS @tech-lead

# Only architects can modify architecture docs
/docs/architecture/ @architects
```

---

### Pull Request Templates

**Default PR template:**

```markdown
# .github/PULL_REQUEST_TEMPLATE.md

## Description
<!-- Provide a brief description of the changes in this PR -->

## Type of Change
<!-- Mark the relevant option with an "x" -->

- [ ] 🐛 Bug fix (non-breaking change that fixes an issue)
- [ ] ✨ New feature (non-breaking change that adds functionality)
- [ ] 💥 Breaking change (fix or feature that would cause existing functionality to change)
- [ ] 📝 Documentation update
- [ ] ♻️ Code refactoring
- [ ] ⚡ Performance improvement
- [ ] ✅ Test update

## Related Issues
<!-- Link related issues here -->

Closes #
Related to #

## Changes Made
<!-- List the specific changes made -->

- 
- 
- 

## Testing
<!-- Describe the tests you ran and provide instructions to reproduce -->

- [ ] Unit tests added/updated
- [ ] Integration tests added/updated
- [ ] Manual testing completed
- [ ] All existing tests pass

### Test Steps
1. 
2. 
3. 

## Screenshots (if applicable)
<!-- Add screenshots to help explain your changes -->

## Checklist
<!-- Mark completed items with an "x" -->

- [ ] My code follows the project's style guidelines
- [ ] I have performed a self-review of my own code
- [ ] I have commented my code, particularly in hard-to-understand areas
- [ ] I have made corresponding changes to the documentation
- [ ] My changes generate no new warnings
- [ ] I have added tests that prove my fix is effective or that my feature works
- [ ] New and existing unit tests pass locally with my changes
- [ ] Any dependent changes have been merged and published
- [ ] I have updated the CHANGELOG.md (if applicable)

## Dependencies
<!-- List any new dependencies or changes to existing dependencies -->

- 

## Breaking Changes
<!-- Describe any breaking changes and migration steps -->

- 

## Additional Notes
<!-- Any additional information that reviewers should know -->
```

**Feature-specific PR template:**

```markdown
# .github/PULL_REQUEST_TEMPLATE/feature.md

## 🎉 Feature Description
<!-- What feature are you adding? -->

## 🎯 Motivation
<!-- Why is this feature needed? -->

## 🏗️ Implementation Details
<!-- How did you implement this feature? -->

### Architecture Changes
<!-- Any changes to system architecture? -->

### Database Changes
<!-- Any database migrations or schema changes? -->

### API Changes
<!-- New or modified API endpoints? -->

## 📸 Screenshots/Demo
<!-- Show the feature in action -->

## 🧪 Testing Strategy

### Unit Tests
<!-- What unit tests were added? -->

### Integration Tests
<!-- What integration tests were added? -->

### Manual Testing
<!-- How to manually test this feature -->

## 📊 Performance Impact
<!-- Any performance considerations? -->

## 🔒 Security Considerations
<!-- Any security implications? -->

## 📚 Documentation
<!-- What documentation was added/updated? -->

- [ ] Code comments
- [ ] API documentation
- [ ] User documentation
- [ ] README updates

## 🚀 Deployment Notes
<!-- Anything special needed for deployment? -->

## 📝 Reviewer Notes
<!-- Specific areas to focus on during review -->

## ✅ Checklist

- [ ] Feature is complete
- [ ] Tests are comprehensive
- [ ] Documentation is updated
- [ ] Performance is acceptable
- [ ] Security review completed
- [ ] Backward compatibility maintained
```

**Bug fix PR template:**

```markdown
# .github/PULL_REQUEST_TEMPLATE/bugfix.md

## 🐛 Bug Description
<!-- What was the bug? -->

## 📝 Root Cause
<!-- What caused the bug? -->

## 🔧 Fix Description
<!-- How did you fix it? -->

## 📊 Impact
<!-- What areas/features are affected? -->

- Severity: [ ] Critical [ ] High [ ] Medium [ ] Low
- Affected users: 
- Affected features: 

## 🧪 Testing

### Reproduction Steps (Before Fix)
1. 
2. 
3. 

### Expected Behavior (After Fix)
<!-- What should happen now? -->

### Test Coverage
- [ ] Unit tests added for the fix
- [ ] Integration tests added
- [ ] Regression tests added
- [ ] Manual testing completed

## 🔙 Backward Compatibility
<!-- Is this fix backward compatible? -->

## 📚 Related Issues
Fixes #

## ✅ Checklist

- [ ] Root cause identified
- [ ] Fix tested thoroughly
- [ ] No side effects introduced
- [ ] Tests prevent regression
- [ ] Documentation updated (if needed)
```

---

## Summary

### Git GUIs

- ✅ **GitKraken**: Professional, visual, team features
- ✅ **SourceTree**: Free, powerful, Git Flow integration
- ✅ **GitHub Desktop**: Simple, GitHub-focused
- ✅ **IDE Integration**: IntelliJ IDEA, VS Code
- ✅ **When to use**: GUI for visualization, CLI for automation

### Git Aliases

- ✅ **Basic shortcuts**: co, br, ci, st
- ✅ **Log aliases**: lg, ls, ll for pretty logs
- ✅ **Workflow aliases**: quick, sync, cleanup
- ✅ **Advanced aliases**: pr (checkout PR), rb (interactive rebase)

### GitHub/GitLab Features

- ✅ **Issues**: Bug tracking, feature requests, project management
- ✅ **Project Boards**: Kanban-style workflow
- ✅ **Actions/CI**: Automated testing and deployment
- ✅ **GitHub CLI**: gh pr, gh issue, gh workflow commands
- ✅ **Protected Branches**: Enforce code review and quality
- ✅ **CODEOWNERS**: Require specific team reviews
- ✅ **PR Templates**: Standardize pull request process

**Best Practices:**

- Use GUI for complex visualizations and conflict resolution
- Use CLI for automation and daily operations
- Create aliases for frequently used commands
- Set up proper CODEOWNERS for critical code
- Use PR templates to ensure consistency
- Leverage CI/CD for automated quality checks
- Protect important branches with appropriate rules
